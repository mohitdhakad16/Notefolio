import { createContext } from "react";  //importing createContext from React

const noteContext = createContext();    // storing the createContext function in the noteContext variable

export default noteContext;